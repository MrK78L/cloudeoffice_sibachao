{
  "name": "orms-backend",
  "private": true,
  "version": "0.1.0",
  "description": "Backend Lambda functions and AWS SAM infrastructure for ORMS.",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "install:business": "npm ci --prefix functions/business-logic",
    "install:image": "npm ci --prefix functions/image-processor",
    "install:notifier": "npm ci --prefix functions/contract-expiry-notifier",
    "install:all": "npm run install:business && npm run install:image && npm run install:notifier",
    "seed": "node seed-data.mjs",
    "seed:check": "node seed-data.mjs --dry-run",
    "seed:local": "node seed-data.mjs --endpoint http://localhost:8000 --table cloffice-offices-table --region ap-southeast-1 --create-table --reset",
    "seed:reset": "node seed-data.mjs --reset --table=cloffice-offices-table --region=ap-southeast-1 --confirm-reset=cloffice-offices-table",
    "test": "node --test tests/*.test.mjs",
    "build": "sam build --use-container --config-file samconfig.toml --no-cached",
    "deploy:dev": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/deploy-dev.ps1",
    "sync:frontend-env": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/sync-frontend-env.ps1",
    "deploy": "sam deploy --config-file samconfig.toml --template-file .aws-sam/build/template.yaml --resolve-s3 --capabilities CAPABILITY_IAM",
    "delete": "sam delete --stack-name cloffice-backend",
    "logs:business": "sam logs --stack-name cloffice-backend --name cloffice-business-logic --tail",
    "logs:image": "sam logs --stack-name cloffice-backend --name cloffice-image-processor --tail"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1084.0",
    "@aws-sdk/lib-dynamodb": "^3.1084.0"
  }
}
